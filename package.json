{
  "name": "@veracioux/website",
  "version": "0.0.1",
  "workspaces": [
    "cli",
    "frontend",
    "frontend/gimmicks/ascii-mugshot",
    "lib"
  ],
  "scripts": {
    "cli": "cd cli && bun run cli",
    "cli:build": "cd cli && bun run build $@",
    "cli:build-publish": "cd cli && bun run build-publish $@"
  },
  "catalog": {
    "puppeteer": "^19.2.2"
  }
}
