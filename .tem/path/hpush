#!/usr/bin/env sh

# Push docker image to heroku

cd "$(tem find -b veracioux-website)"

dgen

push() {
    heroku container:push --app veracioux -R web worker_dotfiles "$@"
}

push || {
    heroku container:login
    push
}
